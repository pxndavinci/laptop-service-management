import { createTheme } from '@mui/material';
export const appTheme = createTheme({
  palette: { primary: { main: '#0d47a1' }, secondary: { main: '#2e7d32' }, background: { default: '#f5f7fb' } },
  shape: { borderRadius: 10 }
});
